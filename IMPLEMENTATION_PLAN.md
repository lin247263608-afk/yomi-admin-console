# 有米出行管理后台实施计划

> 版本：VNext-2026-08-19（以《0819 变更交接》为本轮增量，并对照《管理后台需求文档 v1.0》补漏）
>
> 状态：0819 增量已完成（核心流程代码、类型检查、生产构建与浏览器回归通过）。完整需求文档仍有后续缺口，已单独列入待办，不将“本轮完成”表述为“全量后台完成”。0819 规则与 0818 内容冲突时，以 0819 为准。

## VNext 迭代计划（本轮先执行此计划）

### 目标与边界

本轮将 0819 确认的包车参考金额、独享订单重新下单、优惠券沿用、统计口径和乘客维度增值服务落到可交互后台原型。乘客端的修改、全额支付和优惠券选择不在后台代操作；后台在本地 Mock 原型中呈现修改成功后的两条订单、关联关系、取消原因和退款留痕。

### 0819 决策与缺口

| 领域 | 现行规则 | 本轮后台落点 |
|---|---|---|
| 独享订单修改 | 新单按新信息全额支付；支付成功后生成新单，原单取消并全额退款 | 订单列表保留“原单已取消 + 新单待派单”两条记录，详情可双向查看关联订单 |
| 旧方案失效 | 0818 的“原订单内多退少补 / 差额支付 / 差额退款后直接改写原单”已作废 | 不保留差额计算、补款分支或原单修改中间态；支付失败、超时或放弃时不生成新单，原单保持不变 |
| 关联与退款 | 新单记录来源订单号，原单记录替代订单号；原单全额退款，含增值服务费 | 补充双向关联、`cancel_type = 乘客修改下单信息`、取消原因、退款金额/状态/时间 |
| 优惠券沿用 | 原券默认沿用且不重新校验有效期，乘客可取消或更换 | 后台仅展示新单优惠券快照及其沿用来源，不新增后台代选优惠券入口 |
| 统计口径 | 原单、新单均计入全订单总量与支付订单量；修改产生的取消不计入未来取消率分子 | 在现有驾驶舱口径说明中补充；不新增取消率指标卡 |
| 包车路线 | 参考金额为 GBP 必填数值，仅供乘客端参考 | 列表和新增/编辑统一改为“参考金额”，明确不参与定价、订单、支付或分账，并补齐删除操作 |
| 增值服务 | 按乘客维度记录，项目间不可合并，费用计入分账基数 | 拼车需求主乘客/其他乘客卡片与订单详情“乘客信息” Tab 逐人展示名称、单项费用和小计，无选择时显示“无” |

### 实施顺序

1. **数据模型与演示数据**：增加订单关联、取消类型、退款记录、优惠券沿用来源和乘客维度增值服务；准备可验收的原单/新单样例。
2. **订单列表与详情**：列表保留两条独立记录；详情补充双向关联跳转、取消类型、全额退款留痕和优惠券沿用来源。
3. **拼车与乘客费用**：主乘客和其他乘客分别展示增值服务；人工截团生成订单时保留每位乘客的服务快照，并汇总至订单增值服务费。
4. **包车路线与驾驶舱**：将“展示金额”迁移为必填“参考金额”，补齐删除确认；在既有口径说明中增加修改订单的订单量、支付量与取消率规则。
5. **质量复核**：执行 `npm run typecheck` 与 `npm run build`；通过浏览器回归关联订单、退款、乘客增值服务、包车参考金额及导航路由，并检查控制台无新增错误。

### 本轮验收清单（完成后勾选）

- [x] 订单列表同时可查已取消原单与待派单新单，两者详情可双向跳转。
- [x] 原单显示“乘客修改下单信息”取消类型以及包含增值服务费的全额退款记录；新单显示来源订单和优惠券沿用来源。
- [x] 拼车需求与订单详情均按乘客独立展示增值服务和小计，未选择显示“无”；截团后数据不丢失。
- [x] 包车路线列表和表单使用必填的 GBP “参考金额”，并可完成新增、编辑和删除。
- [x] 驾驶舱仅在静态 Mock 的口径说明中补充 0819 统计规则，未新增取消率指标卡；真实统计计算仍待后端接口接入。
- [x] `npm run typecheck` 和 `npm run build` 通过，相关页面完成桌面端浏览器回归且控制台无新增错误。

> 本轮验收项已按核心代码、构建与浏览器回归结果更新。真实后端接入后仍需补接口联调、权限与审计验收。

### 本轮额外完成的数据一致性修复

- 拼车订单顶层成人/儿童、大/小行李与乘客明细改为同源汇总，避免列表、详情和乘客卡片口径不一致。
- 修改前后的原单、新单使用同一稳定用户身份；用户详情按 `userId` 关联订单，并展示该乘客自己的起终点、人数和行李。
- 已取消订单的司机应得、平台抽佣和结算金额归零；已定价订单退款额不超过实际支付额。
- 连续多次修改时可同时展示来源单和替代单，不再因单侧 `||` 逻辑丢失链路。
- 拼车截团定金按下单组累计；“本单无增值服务但全额退款”使用独立 `fullRefund` 语义，不再误标为按规则退款。
- 包车金额兼容 number 类型表单值，并限制为大于 0 的 GBP 数值；保存后统一格式化为英镑，仅作展示。
- 用户详情演示数据按稳定用户合理分配；优惠券 Tab 合并发放记录和订单用券快照，可追溯 0819 沿用券且保持去重。

### 对照完整需求文档的后续缺口（未纳入本轮完成范围）

以下条目是本轮查漏补缺后的下一迭代候选，当前仍为未完成状态：

1. **账号治理与司机状态共享（P0）**：实现乘客/司机双身份独立冻结、必填原因、通知与日志留痕；司机列表和运力监控改用共享司机 Store，使订单池与冻结状态跨页一致。
2. **司机审核闭环（P0）**：审核详情复用完整司机档案，展示新旧值、证件与驳回原因；审核通过后写回司机列表和详情。
3. **异常订单资金闭环（P0）**：提供专用人工退款、取消退款、金额上限校验、履约/未履约分支、二次确认和处理记录；拼车取消需记录各乘客定金、增值服务和退款金额快照。
4. **财务结算与对账（P0）**：Transfer 仅失败状态可重试；0819 原单退款进入可查询财务流水，司机对账可穿透到订单费用明细，并补充已结算金额口径。
5. **角色、权限与审计（P0）**：补专用角色/账号表单、权限树、超级管理员保护、Mock RBAC、路由守卫和动态审计日志。
6. **订单与调度补漏（P1）**：司导 Tab 补车主 ID、英文名、头像、手机号和车牌；路线、拼车消息改为订单级快照；调度补司机手机号、车型筛选、备注及撤回后重新入公共订单池分支。
7. **筛选与导出（P1）**：财务、对账、异常及日志补日期范围筛选；需求明确的财务与日志导出由 CSV 升级为 Excel。
8. **敏感信息保护（P1）**：司机证件、乘客手机号等敏感信息查看受权限控制，并写入查看日志。

### 已有可复用基线（来自 0818 迭代）

- 成人/儿童、大/小行李与车型双容量已形成类型化数据基础，指派可分别校验座位和行李容量。
- 路线定价编辑器、启用数据源、重复组合/价格校验与特殊时段重叠校验可继续复用。
- 围栏包含/交叠反馈、待派单手动调整抽佣、共享业务配置状态与订单费用分解已具备扩展基础。
- 优惠券由平台承担、增值服务计入分账基数、单座价尾差由平台吸收等 0818 规则仍有效。

### 本轮不做与假设

- 不新增后台修改订单入口、独立退款管理模块、取消率指标卡、后台提现审批、客服 IM 或地图实时监控；但需求内异常订单的人工退款/取消退款仍属于待实现项。
- 原型不执行真实支付、退款、优惠券核销或消息推送；以类型化 Mock 快照验证后台查询与留痕。
- 包车参考金额仅用于展示，不写入订单金额，不触发定价、支付或分账计算。
- 特殊时段命中、围栏面积排序、位置映射三项校验仍由后端返回结果；前端只负责配置校验与错误码/提示展示。
- 当前订单备注与 Toast 仅提供操作反馈，不视为正式审计；动态只读操作日志仍待实现，并在接入审计 API 后以服务端记录为准。

## 目标

以《qoder-04-管理后台需求文档-v1.0.md》为唯一产品边界，先交付可运行、可交互、可持续扩展的 PC 管理后台前端工程，再按业务主链逐批完善页面并接入后端接口。

## 技术方案

- 框架：Vue 3 + TypeScript + Vite
- 路由：Vue Router，按 11 个一级模块组织页面
- 状态：Pinia，承载壳层状态、通知及跨页原型状态
- 图标：`@lucide/vue`
- 样式：原生 CSS Design Tokens + 组件级样式
- 数据：类型化 Mock 层；金额使用整数便士、时间使用 ISO，界面按 `Europe/London` 展示
- 视觉：Geekdance「沉静智库」结构语言 + YOMI 品牌橙 `#F57C00`

## 分阶段实施

### 阶段 0：工程与设计系统（已完成）

- 创建独立前端工程、构建配置与目录结构
- 建立字体、色彩、间距、圆角、阴影和动效令牌
- 建立响应式后台壳层、完整导航、顶部工具栏、Toast、Modal、Drawer
- 建立格式化工具、类型定义与 Mock 数据层

### 阶段 1：核心运营链路交互骨架（已完成）

- 数据驾驶舱：时间维度、周期指标、实时司机快照、导出与刷新
- 拼车需求：筛选、详情、人数限制、人工截团、取消拼团
- 订单管理：派单预警、详情多 Tab、费用拆分、备注、司机调度与价格锁定
- 司机审核：申请类型、变更项、证件状态、整单通过与驳回
- 完成桌面端与 390px 窄屏浏览器验证

### 阶段 2：P0.5 业务模块（待实施）

1. 司导运力看板与列表，落实连接状态和权限状态两个正交维度。
2. 车型、围栏、接送机路线与定价，加入跨字段校验和编辑状态约束。
3. 订单配置、拼车配置与支付配置，加入默认值、校验和生效范围提示。
4. 结算明细与异常订单，覆盖失败重试、部分退款和处理记录。

### 阶段 3：完整后台范围（待实施）

- 用户与司机档案
- 营销管理与运营内容配置
- 客服反馈与异常订单
- 财务对账
- 角色、权限和操作日志

### 阶段 4：接口与上线质量

- 根据后端 OpenAPI 建立请求层、鉴权、错误码和分页协议
- 接入账号密码登录、RBAC、30 分钟无操作退出和敏感信息审计
- 增加单元测试、关键流程端到端测试、可访问性检查和性能预算
- 配置环境变量、CI 构建与英国区域部署策略

## 关键约束

- 需求冲突时以最新定稿文档为准；0819 已明确覆盖 0818 的多退少补、差额支付和原单直接改写方案。
- 独享订单修改必须生成新单并与原单双向关联；只有新单全额支付成功后，原单才取消并全额退款。
- 修改产生的原单、新单均计入全订单总量和支付订单量；`cancel_type = 乘客修改下单信息` 不计入未来取消率分子；营收只统计已完成且已结算订单。
- 包车路线参考金额必填且仅作参考展示，不参与定价、订单、支付和分账。
- 增值服务必须按乘客维度记录与展示，并计入订单分账基数。
- 业务类型固定为“接机 / 送机”，服务类型固定为“拼车 / 独享”。
- 优惠券抵扣由平台承担且不冲减分账基数；订单与对账需同时解释乘客优惠后实付和司机优惠前结算口径。
- 拼车团人数、价格、抽佣和延迟分账等配置在业务实体创建时保留快照。
